# hbd configuration
# See https://github.com/kevinmichaelchen/helix-tools for documentation

[project]
name = ""
default_assignee = ""

[sync]
auto_sync = true
interval_seconds = 5

[embeddings]
backend = "fastembed"
model = "BGESmallENV15"
offline_only = true

[search]
default_limit = 20
