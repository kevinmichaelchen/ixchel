# HELIX-ID AGENTS

**Parent:** See `../../AGENTS.md` and `../AGENTS.md` for shared context.

## Overview

Hash-based ID generation with prefixes and typed wrappers via `define_id!`.

## Structure

```
shared/helix-id/
├── src/lib.rs             # ID types and helpers
└── specs/                 # requirements/design
```

## Where To Look

| Task                  | Location                     |
| --------------------- | ---------------------------- |
| ID format and helpers | `shared/helix-id/src/lib.rs` |
| Usage guidance        | `shared/helix-id/README.md`  |
